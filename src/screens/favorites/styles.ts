import {StyleSheet} from 'react-native';
import { COLORS } from '../../theme/colors';

export const favouritesScreenStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: COLORS.white,
  },


});
